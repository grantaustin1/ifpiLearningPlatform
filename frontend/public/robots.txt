# IFPI Learning — public robots policy (Iter 28)
# The dynamic sitemap lives at /api/seo/sitemap.xml (the K8s ingress
# routes only /api/* to the backend). Crawlers follow the Sitemap:
# directive below and index only opted-in courses + org catalog pages.

User-agent: *
Allow: /catalog
Allow: /verify
Allow: /api/seo/certificates/share
Disallow: /api/
Disallow: /dashboard
Disallow: /admin

Sitemap: /api/seo/sitemap.xml
